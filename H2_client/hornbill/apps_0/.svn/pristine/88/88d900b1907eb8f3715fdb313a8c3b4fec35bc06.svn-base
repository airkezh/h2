@import "block.less";
@import "window.less";
.fluid;
.sysWarningWrap{ background: #FEFFE5; .c; .l(45px); .ml(124px); .mr(124px); .f(14px);
	a{ .tunder(); .cursor(); }
}
.rec_group { background: #FEFFE5; .c; .f(20px); .fb; .l(30px);
	.twoline { padding-left: 256px;
		.btn { .f(12px); .fn; .pr; top: -2px; padding: 3px 10px; }
		.know { .red; .f(12px); .fn; }
	}
}

.poster;
.poster_icons;
.face_pop{.window > .head_float; }

.solidbtn;
.solidbtn > .small;
.solidbtn > .small_red;
.solidbtn > .small_pink;
.solidbtn > .big;

.repinNotic { .common_shadowBox; .mt(14px); .p13; .w(200px);
	ul { .mt(5px);
		li { border-bottom: 1px solid #ddd; .gray; .block; .l(16px); .hidden; .p8_0; width: 100%;
			span, a { .mr(5px); }
			&:hover { background: #f2f0f0; }
			.face {.avatar32; }
		}
		.last { .border-n; }
	}
}

.common_title;
.groupCon{ .group > .9GG; .hidden;
	.small_btn { .w(88px); }
	.paper { .c; .mt(10px); }
	.big_btn { margin: 8px 0 8px 12px; .w(190px); }
}
.groupBox_change {
	.imgBox { .h(298px); .w(226px); }
}
.interest { .common_shadowBox; .mt(14px); .hidden; padding: 0 13px 13px;
	li { .h(48px); .mt(13px); .hidden;   
		p { .ml(56px); .w(140px); .nowrap; .ellipsis; .hidden; }
		.folbtn { .left; .h(18px); .l(18px); .w(60px); }		
	}
}
.h_update { .pink_update; }

.rec_pager { .c; .mt(10px);
	.small_btn { .w(140px); }
}
.pageNav{.page > .pageRed; }

