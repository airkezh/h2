@import "block.less";

.container_ydt { .pb(10px); .auto; .w(960px); .f(14px);}
.content_ydt { .w(708px); .left; .inline; .ml(6px); .mr(6px); 
	dl { .pt(30px);}
	dt { .fb; .f(16px); }
	dd { .l(22px); text-indent:2em; }
	h1 { .f(20px); }
}
.sidebar_ydt { .common_box; .f(12px); }
.frame_ydt { .common_shadowBox; .p13; .mt(14px); 
	a { .mt(8px); .red; .block; .l(20px); }
	a:hover { .tunder; }
}
#one {
	a { .inline; }
}
